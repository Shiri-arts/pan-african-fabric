---
interface Props { width?: 'standard' | 'editorial' | 'gallery'; class?: string; }
const { width = 'standard', class: className } = Astro.props;
---
<div class:list={['container', `container--${width}`, className]}><slot /></div>
